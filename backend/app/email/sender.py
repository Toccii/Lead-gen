"""Sends and reads mail via Microsoft Graph API (application permissions, single mailbox).
Supports a dry-run mode that generates and logs the email without actually sending it.
Implemented in Phase 3 (send) and Phase 4 (read/reply detection).
"""
